# ============================================================
# WAN Video Generation & Latent Exploration
# Python 3.10+ required (tested on 3.12.9)
# ============================================================

# ------------------------------------------------------------------
# PyTorch — install SEPARATELY with the correct CUDA index URL.
# See the README for the exact command. Do NOT install from this file
# if you need CUDA support.
# ------------------------------------------------------------------
# torch>=2.6.0
# torchvision
# torchaudio

# HuggingFace model stack
# These three are tightly coupled — pin to known-working versions.
# diffusers 0.34.0 requires transformers <4.56 (FLAX_WEIGHTS_NAME removed in newer versions).
diffusers==0.34.0
transformers==4.55.2
accelerate==1.10.0

# Video / image I/O
opencv-python>=4.8.0
Pillow>=10.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.5.0

# Numerical & scientific computing
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.4.0

# Plotting & visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# VLM (Qwen-VL) utilities
qwen-vl-utils>=0.0.8

# Misc utilities
tqdm>=4.66.0
PyYAML>=6.0
ftfy>=6.0.0          # text normalization used by diffusers' prompt cleaning

# ---- Web interface (webapp/backend) ----
Flask>=3.0.0
Flask-CORS>=4.0.0

# ---- Optional / lazy-imported ----
# Uncomment if you use UMAP trajectory visualizations:
# umap-learn>=0.5.6

# Uncomment if you use video similarity analysis (SSIM/MSE):
# scikit-image>=0.22.0
